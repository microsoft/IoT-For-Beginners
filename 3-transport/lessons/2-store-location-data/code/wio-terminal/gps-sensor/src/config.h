#pragma once

#include <string>

using namespace std;

// WiFi credentials
const char *SSID = "<SSID>";
const char *PASSWORD = "<PASSWORD>";

// IoT Hub settings
const char *CONNECTION_STRING = "<CONNECTION STRING>";